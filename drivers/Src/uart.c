#include "uart.h"

void UART_Init(void)
{
    // UART initialization stub
}

void UART_SendString(const char *text)
{
    (void)text;
}

uint16_t UART_Receive(char *buffer, uint16_t size)
{
    (void)buffer;
    (void)size;
    return 0;
}
